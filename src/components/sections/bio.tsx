"use client";import{motion}from"framer-motion";import{MapPin,Calendar,Code,Layers,GraduationCap,Phone,MessageCircle}from"lucide-react";import{portfolioData}from"@/config/portfolio.data";const statIcons=[Calendar,Code,Layers,MapPin];export function BioSection(){const p=portfolioData.personal;return(<section className="section-padding"><div className="section-container"><div className="grid lg:grid-cols-5 gap-8 lg:gap-12 items-start"><motion.div initial={{opacity:0,x:-40}} whileInView={{opacity:1,x:0}} viewport={{once:true}} className="lg:col-span-2 glass-card p-6 md:p-8 space-y-5"><div className="w-20 h-20 rounded-2xl bg-gradient-to-br from-primary/30 to-purple-500/30 flex items-center justify-center"><span className="text-3xl font-bold text-primary">{p.firstName[0]}{p.lastName[0]}</span></div><div><h3 className="text-xl font-bold">{p.firstName}</h3><p className="text-sm text-primary mt-0.5">{p.username}</p><p className="text-sm text-muted-foreground mt-1">{p.title}</p></div><p className="text-sm text-muted-foreground/80 leading-relaxed">{p.bioTagline}</p><div className="flex items-center gap-2.5 text-sm text-muted-foreground"><GraduationCap size={16} className="text-primary shrink-0"/><span>{p.education}</span></div><p className="text-sm text-muted-foreground">{p.roleLine}</p><div className="flex flex-col gap-2 pt-2 border-t border-white/[0.06]"><a href={`https://wa.me/${p.whatsapp.replace(/[^0-9]/g,"")}`} target="_blank" rel="noopener noreferrer" className="flex items-center gap-2 text-sm text-muted-foreground hover:text-green-400"><MessageCircle size={14}/>{p.phone}</a><a href={`mailto:${p.email}`} className="flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><Phone size={14}/>{p.email}</a></div><div className="grid grid-cols-2 gap-3 pt-1">{portfolioData.bio.stats.map((s,i)=>{const Icon=statIcons[i]||Calendar;return(<div key={s.label} className="p-3 rounded-xl bg-white/[0.03] border border-white/[0.06]"><Icon size={14} className="text-primary mb-1"/><div className="text-lg font-bold">{s.value}</div><div className="text-xs text-muted-foreground">{s.label}</div></div>)})}</div></motion.div><motion.div initial={{opacity:0,x:40}} whileInView={{opacity:1,x:0}} viewport={{once:true}} transition={{delay:0.2}} className="lg:col-span-3 space-y-6"><p className="text-sm text-muted-foreground uppercase tracking-widest">{portfolioData.bio.title}</p>{portfolioData.bio.paragraphs.map((para,i)=><p key={i} className="text-lg md:text-xl text-foreground/80 leading-relaxed">{para}</p>)}<div className="inline-flex items-center gap-2 px-4 py-2 rounded-xl bg-primary/10 border border-primary/20 text-sm text-primary font-medium">{p.teamName}</div></motion.div></div></div></section>)}