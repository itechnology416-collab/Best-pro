"use client";import{motion}from"framer-motion";import type{LucideIcon}from"lucide-react";import{Cpu,Globe,Shield,Cloud,Server,Sparkles,Zap,Wand2}from"lucide-react";import{portfolioData}from"@/config/portfolio.data";const iconMap:Record<string,LucideIcon>={Cpu,Globe,Shield,Cloud,Server,Sparkles,Zap,Wand2};export function Specialties(){return(<section className="section-padding"><div className="section-container"><motion.div initial={{opacity:0,y:20}} whileInView={{opacity:1,y:0}} viewport={{once:true}} className="text-center mb-14"><p className="text-sm text-muted-foreground uppercase tracking-widest mb-2">Specializing In</p></motion.div><div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3 md:gap-4">{portfolioData.specialties.map((s,i)=>{const Icon=iconMap[s.icon]||Cpu;return(<motion.div key={s.label} initial={{opacity:0,y:30}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:i*0.05,duration:0.4}} whileHover={{y:-4,scale:1.02}} className="glass-card glass-card-hover p-5 flex flex-col items-center text-center gap-3 cursor-default"><div className="p-3 rounded-xl bg-primary/10 text-primary"><Icon size={22}/></div><h3 className="text-sm md:text-base font-semibold text-foreground">{s.label}</h3></motion.div>)})}</div></div></section>)}